#pragma once

#include <iostream>
#include <string>
#include <sstream>
#include <cassert>

std::string numSufFSimp(int target);
std::string number_suffix(int target, bool returnNumToo = false);
